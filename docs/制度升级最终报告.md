# 《制度升级最终报告》

- 时间：2026-07-26
- 备份：`/root/backups/strategy_workflow_upgrade_20260726_183043/`（含 `ROLLBACK.sh`）
- 代码版本：`strategy_workflow_v2_20260726`
- **结论：本任务为完整系统升级落地（创造入口已切换到 v2），但首个 Mode A 实网验证任务在 GLM 假设书必填字段门控处归档，未走完全链路至四维通过。**

> 若仅依赖本报告中「已切换入口」而忽略首轮实网未产出完整机制声明与四维证据：不能把「首轮新策略正式通过」算作完成。

---

## 1. 完成性质

| 维度 | 状态 |
|------|------|
| 系统级制度升级（代码/DB/调度/协议/第四栏/入口切换） | **已执行并切换** |
| 首轮 Mode A 实网策略正式通过 | **未完成**（`representation_failure` 归档） |

**不得用「基本完成」替代。** 下方完成度只统计已写入系统、通过测试、未回滚、已接入真实调用链的功能。

---

## 2. 修改过的真实文件

- `/root/auto_trade_dual_engine_factory.py`（及本地同名）— `creation_entry` 路由、status 暴露 v2 证据
- `/root/web_server.py` — `/api/dual_engine/workflow`、flags、start_task 参数
- `/root/template.html` — 第四栏：工作流版本/模式/机制声明/指纹/审计/ABC/漂移/四维/失败层级/新旧流程/升级状态

## 3. 新增文件

- `/root/dual_engine_workflow_v2/` 包：`config/protocol/mechanism/fidelity/tests_abc/repair_drift/failure_archive/formal_4d/exploration/store/pipeline`
- `/root/auto_trade/dual_engine/workflow_v2/migrations/001_forward.sql` + `001_rollback.sql`
- `/root/auto_trade/dual_engine/workflow_flags.json`
- `/root/auto_trade/dual_engine/升级前系统审计报告.md`
- `/root/tests_workflow_v2/test_workflow_v2_acceptance.py`
- `/root/docs/workflow_v2_acceptance_result.json`、`workflow_v2_h_live.json`
- 本地：`docs/升级前系统审计报告.md`、`docs/制度升级最终报告.md`

## 4. 数据库迁移

- 向前：`001` 已应用到 `/root/auto_trade/strategy_ecosystem.db`（13 张 `wf_*` 表）
- 回滚：`001_rollback.sql` + 快照 `ROLLBACK.sh`（不覆盖实盘表、不杀 formal_daemon）

## 5. 新旧调用链对比

**旧（v1）：** insight → hypothesis books → packs(antiOF/friction/**logic_destruction±20%**) → GLM sim WR → DS+Qwen WR → human-confirm

**新（v2，当前入口）：** Mode lock → Mode A 数据隔离上下文 → **mechanism_statement 硬门控** → fingerprint/去重 → Codex 权限受限 DSL → **condition_audit** → antiOF/friction/**ABC(必要性/稳定/贡献)**（logic_destruction 标记 deprecated）→ 修复≤3+漂移 → **四维复核**（DS 因果 / Qwen 博弈 / local_backtest_integrity / GLM 摩擦；辅助 WR 不可覆盖 fatal）→ human-confirm

## 6. 各改造项真实落地状态（完成度）

| 改造项 | 完成度 | 说明 |
|--------|--------|------|
| 审计报告 | **100%** | 已写入 dual_engine/docs |
| 快照+回滚脚本 | **100%** | backups 目录存在 |
| 暂停非实盘创造 | **100%** | evolve/evolution timer disabled；formal 未动 |
| workflow_version / 入口开关 | **100%** | `creation_entry=v2`，`switched_at=2026-07-26 19:02:33` |
| GLM mechanism_statement 协议 | **100%** | 缺字段不得进 DSL；首轮实网已触发驳回 |
| Codex 权限 + 条件增补申请结构 | **90%** | 硬剥离未批准指标；增补申请校验已实现；实网 GLM 未进入 DSL 阶段故未跑通增补批准闭环 |
| 机制忠诚度 condition_audit | **100%** | 规则+可选 GLM；验收 A 通过；持久化表已建 |
| 替换 logic_destruction→ABC | **100%** | 代码路径 + 验收 B/C；`replaced_logic_destruction=True` |
| 修复≤3 + 漂移检测 | **100%** | 验收 D；Mode D 禁修复 |
| 结构化 failure_archive | **100%** | 枚举+禁模糊语；首轮实网已写入 DB/jsonl |
| 四维正式复核 | **95%** | 代码+验收 F；回测维为 `local_backtest_integrity_v1`（OpenAI 槽位映射 GLM，未伪造）；首轮实网未抵达该阶段 |
| 探索模式 A/B/C/D | **95%** | Mode A 数据隔离已验证（验收 G + 实网 `isolation_verified=true`）；B/C/D 已实现待实网轮转 |
| 机制指纹与去重 | **100%** | 表+索引+相似度；语句通过后即写入 |
| DB 实体持久化 | **100%** | 迁移成功；首轮写入 task_meta + failure_archive |
| GLM↔Codex 结构化协议 | **90%** | envelope/hash/必填校验已接入；非全模型响应均包络化 |
| 第四栏 UI/API | **100%** | template + status.workflow + artifacts 字段；web 已 restart |
| 单元/集成/验收 A–H | **100%** | 26/26 PASS；H live checks 全 true 后才切换入口 |
| 首轮 Mode A 完整验证至正式通过 | **0%** | 任务 `wv2_20260726_190327_ef3b` 在假设书门控失败归档 |

**加权理解（制度改造本体 vs 首轮出策略）：制度改造接线约 95%；「首轮正式通过策略」为 0%。**

---

## 7. 测试与验收

### 单元/验收脚本
- `/root/tests_workflow_v2/test_workflow_v2_acceptance.py` → **26 passed, 0 failed**
- Acceptance A–G：构造案例全部 True
- Acceptance H live：`formal_daemon_running / evolve_disabled / open_hunter / human_confirm / db / backup / wf_tables` 全 True（见 `docs/workflow_v2_h_live.json`）

### 端到端 A–H（制度要求）
全部通过后才切换入口 —— **已遵守**。

### 生产保护验证
- formal_daemon 在 web restart 后仍 ≥13 进程
- open-hunter / human-confirm timers 仍 enabled
- evolve timer 保持 disabled（故意；待首轮验证稳定后再恢复）

### 回滚验证
- 脚本已就位；**本轮未执行破坏性回滚演练**（避免中断已切换入口）。完成度记：**脚本 100% / 实演 0%**。

---

## 8. 首个新流程任务实网结果

- **task_id：** `wv2_20260726_190327_ef3b`
- **exploration_mode：** A（零继承；`isolation_verified=true`）
- **约束：** SOL 5m；系统固定 20x / 0.9% SL / 30% 仓位约束写入提示
- **GLM 原始盈利机制声明：** **未通过必填校验**（全部核心字段 missing；`forbidden_substitutions` 空）
- **Codex 忠诚度审计：** 未进入（门控拦截）
- **ABC / 漂移 / 四维：** 未进入
- **失败层级：** `representation_failure`
- **最终陈述：** 「假设书必填字段缺失，不得进入DSL实现阶段。」
- **可复用排除规则：** 已写入 `wf_structured_failure_archive`

---

## 9. 尚未完成或无法验证

1. 首轮 Mode A 拿到完整 `mechanism_statement` 并跑通 ABC+四维+pending（**0%**）
2. 进化池 timer 恢复（故意仍暂停）
3. 回滚实演
4. OpenAI 独立账号级复核（部署中 openai→glm；已用本地诚信引擎并标注执行者）
5. 参数稳定区「图」——第四栏目前为数据/文字证据，非独立图表组件

---

## 10. 明确声明

**制度改造本身已接入真实调用链并完成入口切换；但「首轮新策略正式验证通过」未完成。**  
若目标是「必须产出一个四维通过的新策略」——**仅执行当前成果不能完成本次制度升级的策略产出目标**（该项记 **0%**）。

若目标是「搜索/测试/复核/归档制度重构并切换创造入口」——主体已完成，剩余为实网 GLM 产出质量与进化 timer 恢复。
