# Gap-fill acceptance（2026-09-07）

## Intended closes
1. Actions YAML → `.github/workflows/` (needs `workflow` OAuth scope to push)
2. Sync `kimi_provider` spend-brake (`KDH_DISABLE_*`) to GitHub
3. Update hub topology docs + other-Cursor handoff
4. Branch protection on `main` (blocked on private free unless Pro/public)
5. Tag + refresh `/root/deployed.sha` after merge

## Remaining if any
- See checklist at bottom of this PR / final chat report
