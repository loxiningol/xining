{
  "pack_path": "/root/strategy_macro_sfp_unblocked_v1.json",
  "symbol": "ETH-USDT-SWAP",
  "timeframe": "15m",
  "direction": "long",
  "tag": "auto_driver_macro_sfp_unblocked_v1",
  "max_iterations": 6,
  "l1_seed_retries": 4,
  "ai_providers": ["deepseek", "qwen", "glm"],
  "prefer_provider": "glm",
  "ai_max_tokens": 2800,
  "convergence_eps": 0.02,
  "convergence_patience": 3,
  "stagnant_reason_patience": 4,
  "max_kb_family_bumps": 3,
  "auto_bump_family_after_gate2": true,
  "enable_multi_symbol_matrix": true,
  "report_name": "delivery_report.md",
  "report_copy_to": "/root/delivery_report_macro_sfp_unblocked_v1.md",
  "workdir": null,
  "vector_root": "/root",
  "dry_run": false,
  "optimize_goals": [
    "family=macro_sfp_unblocked_v1 — lightweight funnel v3: L0 density → BTC/ETH/SOL anchor L1 → RAM-budgeted matrix",
    "NEVER rename into exact blocked family macro_sfp_displacement (KB exact match)",
    "keep Macro SFP core: low<pdl & close>pdl (long); high>pdh & close<pdh (short); displacement body close vs open",
    "vol_z20 floor for THIS family is 0.8 — may tune in [0.5, 1.2]; do NOT stack extra AND filters that zero L0 density",
    "If L0 REJECT_TOO_RARE: relax non-causal filters first (vol_z), never delete PDL/PDH sweep+reclaim core",
    "Matrix width is RAM-adaptive on this host — do not demand full 38 if soft-kill/calmar budding fails",
    "Soft budding guidance (payoff≥1.3 / calmar≥0.8) is for AI progress only; formal Gate2 floors stay absolute",
    "exit: keep scale-out partial_tp_atr≈0.5@2.0ATR + atr_trailing≈4.5; swing_extreme lookback≥5; no fixed % TP",
    "never_lower_gate2_floors: payoff≥2.5 / calmar≥1.5 / WF≥7/10 / worst5 floors stay absolute",
    "never mount / human confirm; success only enters pending queue"
  ],
  "notes": [
    "Lightweight funnel v3: memory guard + L0 density + anchor L1 before any wide matrix.",
    "Formal Gate2 floors unchanged. Soft incubation bands are guidance only.",
    "764MB host: matrix auto-degrades to primary/anchor when avail RAM is low to protect qiyu-web."
  ]
}
