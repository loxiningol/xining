# 单一创造系统（强制）

完整约定见 `docs/PARALLEL_CREATE_HUBS.md`（标题保留，内容已改为单系统）。

## 现网

- **唯一** invent：`qiyu-kimi-thin-hub` → `scripts/kimi_thin_recipe_loop_hub.py`
- 队列 `/tmp/kdh_thin`；Mac worker 只盯这一根（建议 8 路）
- 车道：`1→kimi1(primary)`，`2→kimi2(backup)`；`KDH_INVENT_STRICT_BIND=1`
- **禁止**再启 `qiyu-kimi-thin-hub-b` / `/tmp/kdh_thin_b` / worker-b

## 分支

- 创造主链改动用 `cursor/shared-*`
- `cursor/hub-a-*` / `cursor/hub-b-*` 废弃

## 共享文件不经 PR 不上 VPS

改下列任一后禁止直接当正式发布（紧急 scp 须封印）：

- `dual_engine_workflow_v2/kimi_provider.py`
- `dual_engine_workflow_v2/invent_*.py`
- `scripts/kimi_thin_recipe_loop.py`
- `scripts/kimi_thin_recipe_loop_hub.py`
- `scripts/research_host_eval_worker.py`
- `scripts/kimi_dual_http_create_20260906.py`

## 验收 Python 路径

`cd /root && PYTHONPATH=/root`，禁止在 `/home/admin` 下 import 交易模块。
