# 栖语自主学习型策略生命生态 V2

## 数据闭环

1. 每 5 分钟采集各实盘标的/周期的闭合 K 线、指标、形态与市场环境。
2. 每个策略每根闭合 K 线保存触发结果；未触发时保存单参数反事实近失原因，DSL 保存精确条件明细。
3. 开仓、挂载止损、止损确认、平仓回执与交易所对账被合并为同一个实盘案例；活动案例在平仓后原位更新为完整案例。
4. 参数阈值以统一回测做反事实归因；DSL 逻辑以增加、删除、阈值变异进行条件级归因。
5. DeepSeek、Qwen、ChatGPT 先并行且彼此不可见地提出结构化假设，分别偏重形态/环境、条件反事实、生命周期/实盘风险。
6. 独立研究结束后进入匿名交叉会审：参数方向必须至少由两个 AI 独立提出且获得至少 2/3 会审支持；单一来源的新 DSL 逻辑必须获得 3/3 会审支持，才有资格晋级。未达成一致但结构合法的想法仍可进入隔离探索回测并形成成功/失败经验，绝不能晋级版本或实盘。
7. 参数候选和 DSL 候选使用相同指标、相同时间区间、20 倍杠杆、手续费与滑点，按 0.9% 主止损和 0.6% 辅助止损回测；探索权与晋级权相互分离。
8. 样本、胜率、期望、回撤、连损和留出段通过后，三 AI 再对同一候选哈希独立复核；缺席、错误或任一否决均不通过。
9. 数值参数只允许在声明范围内改变一个步长参数；杠杆、止损、仓位和组合风险字段永不可由 AI 修改。
10. 新逻辑只能使用无代码执行能力的 `qiyu_strategy_dsl_v1`。三 AI 一致通过后仍须人工批准；启用实盘还需单独的 `--enable-live`。
11. 实盘持续比较版本回测预期、最近 5/10/15 笔实际胜率、止损簇与当前市场环境：短窗异常预警，多窗恶化进入衰减观察，三窗全部恶化且环境不匹配才暂停并回滚。

## AI 分工

- DeepSeek：形态结构、市场环境切换、成功与失败案例差异。
- Qwen：条件贡献、反事实敏感性、胜率与频率的权衡。
- ChatGPT：生命周期、回测—实盘偏差、风险收益和执行可行性。
- 三者先独立发散，再对匿名假设分别投票；会审员不能改写或新建假设，也看不到其他会审员的票。通过确定性回测后，三者再独立审核完全相同的候选哈希与证据。

## 失败记忆

- 回测门槛失败、DSL 校验失败、AI 否决和实盘衰减均写入 `failure_experiences`。
- 已测试候选按规范哈希去重；DSL 另按剔除名称与署名后的可执行逻辑哈希去重，避免换名重提。
- 失败原因和历史条件贡献会进入下一轮研究上下文。

## 运维入口

- 高频采集：`qiyu-strategy-experience.timer`，每 5 分钟，不调用 AI。
- 深度研究：`qiyu-strategy-ecosystem.timer`，每日北京时间 00:20。
- 新策略创造：`qiyu-strategy-creator.timer`，每 2 小时由生产率元控制器动态分配算力；单次进程设有 CPU/内存上限，完成后退出释放内存。
- 外部 AI 调用还必须通过 root-only 授权记录 `/root/auto_trade/ai_research_consent.json`；缺失、损坏、供应商或用途不在授权范围内时一律拒绝调用与晋级。
- 状态：`python3 /root/auto_trade_strategy_ecosystem.py --status`
- 人工批准研究版 DSL：`python3 /root/auto_trade_strategy_ecosystem.py --approve-version <哈希>`
- 人工批准并启用实盘：`python3 /root/auto_trade_strategy_ecosystem.py --approve-version <哈希> --enable-live`
- 网站只读状态接口：`GET /api/strategy-ecosystem/status`（沿用网站认证）。

研究与采集服务共用文件锁，避免研究回测期间生命周期任务同时修改版本。生命周期暂停只阻止新开仓，绝不撤销已有持仓的确定性止盈、止损和定时平仓管理。

网站“策略创造训练”模块只展示已经通过匿名会审、统一双止损回测和最终三 AI 同哈希一致复核的 DSL 候选。该模块没有实盘批准按钮；候选必须由用户通过 Codex 明确确认，随后才可执行 `--approve-version ... --enable-live`。持续训练服务自身没有自动实盘晋级权限。

## 成本感知搜索与算力剪枝（2026-07-22）

1. 净收益从搜索第一阶段起就同时扣除账户实际 taker 手续费、滑点、半价差、市场冲击、延迟与持仓资金费率；同一候选还必须通过 observed、stressed、severe 三档成本情景。
2. 只读校准器每 15 分钟记录每个交易标的五档盘口深度与价差、实际成交相对标记价格偏差、最近资金费率，并用“账户权益 × 20 倍 × 最大 S 级 70%仓位”形成容量参照。五档样本未满 24 个前继续使用保守冲击代理，不以单次盘口冒充历史流动性。
3. 目标事件的每个“方向 × 交叉事件 × 市场环境”分支都有 60 日时间衰减的分层 Beta 生存后验；后验严格使用同标的同周期证据，跨标的和跨周期只允许描述性比较，不转移结果标签。同一标的、周期、分支在同一自然周内的高度重叠重复运行只计一次后验证据，避免伪样本量膨胀；原始运行仍完整保留审计。冷启动以高不确定性保留探索，但首次基线最多只允许 2 个分支进入完整评估。
4. 获取函数同时考虑历史后验均值、不确定性、新颖度、AI 共识、计算量和历史死路惩罚。它是可解释的成本约束 posterior-UCB，不伪装成当前样本量尚不支持的复杂黑箱元模型。
5. 参数候选与事件图谱都按整批 Successive Halving 运行：先统一做最近 90 日净成本快筛，再仅将前 20%（受绝对预算上限约束）送入全历史、双止损、压力成本、前向分段和跨市场检验。预算淘汰与实证失败分开记录，避免把“没拿到算力”误当成“已证伪”。
6. 所有进入昂贵阶段的并行候选使用 Bayesian FDR `q=0.10`，按 Bootstrap 后验符号错误概率控制入选集合的平均错误率；这里不把后验概率冒充经典 p 值。随后仍须通过时间封存留出段，不能用多重检验替代真正的样本外验证。
7. 每轮搜索把分支观察、存活率、连续拒绝、获取函数指数移动值、快速候选数、全量候选数及节省的 AI 调用写入数据库。生产率元控制器据此把预算移向高产但仍有新颖度的方向，并降低重复死路的探索权重。
8. 深度搜索资格只认同标的、同周期、已经覆盖净摩擦成本的实测幸存分支。快速阶段“看起来有希望”、未标注微观聚类活跃或信息增益高，都不能购买三 AI 抢救调用。完成至少 48 条基线分支、没有成本幸存者且已提前剪枝的集群进入 7 天可审计休眠；休眠只停止主动创造，前向数据采集继续，人工显式运行仍可唤醒。
9. 研究状态持续公布休眠/活跃/冷启动集群数量及预算治理规则。数据库清理和慢微观重算降频必须先取得服务器真实文件大小、查询延迟和新增稳定基元产率，不能为了“节能”盲删认知证据或降低观察频率；这些动作与实盘执行链完全隔离。

边界说明：部署前没有保存的历史 L2 盘口、队列位置、逐单延迟和部分成交轨迹无法事后重建。系统会随前向样本累积逐步启用容量冲击校准；在达到样本门槛前明确标记为“收集中”，不会声称已经拥有不存在的数据。

## 主动狩猎式正向样本生成（2026-07-22）

1. 只有当普通事件图谱虽未形成最终可信方向、但至少存在一条同标的同周期的净成本幸存分支时，系统才把真实图谱中信息量最高的最多 6 个模式和一份受控、可计算过滤器目录分别交给 DeepSeek、Qwen、OpenAI。若成本幸存分支为 0，系统直接记录确定性死亡证据并提前结束，禁止 AI 为无实证边缘的方向“抢救”。三者必须逐项给出必要性判断，不得新增系统没有的数据字段。
2. 一个方向只有在完整三 AI 面板中至少 2 个给出 `HUNT`、必要过滤器至少获得 2 票、没有可测量硬伤否决，且流动性恶化、参与者学习/拥挤、环境切换三轮适应性攻击均存活，才获得一次探针预算。AI 意见分歧度只用于提高信息增益，不会绕过最低生存门槛。
3. 探针只在事件与必要过滤器全部同时满足的窄窗口运行，按照实际观测摩擦的 3 倍、20 倍杠杆和 0.9% 止损计算。至少 8 个精确事件、3 个时间后段事件、净均值为正且后验正均值概率不低于 55%，才标记为 `suspected_positive_probe`。它仍不是已验证策略，也不能直接实盘。
4. 理论完美场景只保存为 `unlabeled_search_prior_not_positive_sample`。当精确窗口不足时，系统可以查找满足至少三分之二必要条件的历史邻近样本作为富矿区线索，但相似样本永远不能替代精确窗口或成为正例标签。
5. 三 AI 生存先验按标的/周期每自然周最多生成一次并缓存；失败的供应商调用不缓存，下一轮可重试。每次探针都写入实验表；`suspected_positive_probe` 只存在于实验表，不进入正例核心库。核心库只接纳完成全部离线关卡与至少七日前向影子验证的 `verified_shadow_positive`，并明确 `synthetic_label=0`；之后仍不能绕过 Codex 代码复核与人工实盘批准。
6. `/root/auto_trade_microstructure_collector.py` 每分钟启动一次受限只读进程，采集五档深度、价差和最近 100 笔公共成交，保存深度不平衡、主动买卖流不平衡及窗口内加速度摘要，保留 35 天。它不是 200 毫秒 OFI，也没有队列位置；在形成足够前向历史并能保持训练/留出时间隔离以前，不进入策略证明链。
7. 外部模型的自然语言或 JSON 包装错误只允许进入“格式修复/定向补答”，不能降低实质门槛：进入探针仍要求三家结果完整，`HUNT` 仍要求三轮攻击齐全；`REJECT` 可省略三轮说明以降低输出截断概率。任何一家最终缺席都按未达成共识处理。
8. 探针成败以有界权重回写分支 Beta 后验，并随 60 日半衰式时间权重逐渐淡化；同一标的、周期、模式、过滤组合在 7 天内直接复用已有探针，不重复回测。最近探针历史会回送下一轮三 AI，失败组合若没有新的可测量依据不得换一种措辞重新提出。

## 失败驱动的受控变异与正例四关（2026-07-22）

1. 变异不允许自由生成指标。三 AI 只能在登记的反死因算子中投票；算子由系统编译为 OHLCV 可观测过滤器，例如动量脉冲、强方向实体、两根连续确认、1 小时环境同向、非末端振荡和可交易波动。至少两票才进入探针，任何 1 秒/5 秒、排队、撤单或历史 L2 叙事不能编译。
2. 每轮最多选择一个信息量最高的新失败探针进行解剖。DeepSeek 可使用深度推理定位唯一主死因，但只能从受控过滤器目录提出一条 `REQUIRE` 规则；没有证据必须返回空规则。
3. 规则先在相同历史事件上接受三倍全摩擦影响审计：原事件至少 12 个、保留至少 8 个、被剪至少 3 个、剪除率不超过 80%，且保留组净均值必须同时优于原组与被剪组。随后 Qwen 和 OpenAI 必须双双批准。通过后仍先隔离；同一规则在两个独立失败实验中重复出现才激活。
4. 离线正例确认第一关是五个重叠滚动时间窗，至少三个窗口有样本且 60% 以上保持正期望；第二关是历史高波环境与 severe 成本尾部压力，限制回撤和连损；第三关要求年化机会数不少于 30，并已累积至少 24 个深度样本且建模仓位占保守五档深度不超过 25%。
5. 第四关由 `/root/auto_trade_shadow_validator.py` 每五分钟处理新闭合 K 线，至少观察七个自然日和五笔完整交易。它使用当时可得的分钟盘口摘要与账户校准 taker 成本，完全没有交易所下单接口。只有影子净期望为正、胜率不低于 55%、最大连损不超过 3 且每个退出均有同期微观摘要，才进入正例核心库并等待 Codex 复核。
6. 影子环境无法复原队列位置、撤单概率和亚分钟盘口路径，因此不会宣称“真实排队仿真”。这些能力必须等未来更高频的前向采集形成足够时间隔离样本后再启用。

## 死亡地形、慢微观基元与影子生存包络（2026-07-22）

1. 每轮事件图谱的全部确定性淘汰分支写入 `prescreen_rejections`，保存净期望、留出段、压力成本、止损簇和多重检验等数据证据。只有排名靠前并真正送入三 AI 面板的分支才另存 `three_ai_prescreen` 记录，保留供应商、原始理由、硬否决与投票；系统不得把 AI 推理表述成48个分支都经过了AI会审。
2. 相同标的、周期、模式和死因按周形成 `prescreen_death_maps`。DeepSeek只负责把已经记录的理由压缩成可检索地形描述，不能修改确定性标签。下一轮变异读取死亡地图；重复死因若没有至少两个 AI 共同选择能够覆盖该死因的登记算子，则直接失去探针资格，并在获取函数中承担历史死路惩罚。
3. `/root/auto_trade_microstructure_primitives.py` 每六小时读取前向公共微观遥测，用连续五个分钟快照构造慢微观窗口。特征仅含价差、五档总深度与不平衡、最近成交方向与加速度及5秒/15秒/60秒子窗口；使用按标的稳健标准化与确定性聚类，不读取未来价格、策略、开平仓或盈亏标签。
4. 新聚类先由 DeepSeek给出中性可观测名称，再由 Qwen 与 OpenAI 双重审计。任何价格预测、盈利/胜率、交易指令、做市商意图、吸筹护盘、撤单或排队叙事都会拒绝注册。通过者仅成为 `descriptive_registered` 描述词典，不能作为正例、历史回测过滤器或实盘信号。
5. 影子验证对每个新闭合K线另存 `shadow_environment_snapshots`：滚动ATR分位、成交量分位（存在时）、EMA位置、同期微观摘要及最近注册基元。快照只接受10分钟内的公共微观样本；过期数据明确记为不可用，不能用旧盘口冒充同期环境。
6. 影子完成后按“波动环境 × 慢微观基元”生成描述性的生存包络，记录交易数、胜率与净期望。该包络用于未来环境降级和失败归因，不构成因果证明，也不能绕过七日、五笔、成本、连损、Codex与人工批准门槛。
7. 系统的研究哲学固定为“慢微观鲁棒性”：候选必须在分钟快照与5秒/15秒/60秒采样下仍可观察；凡依赖亚5秒速度优势、队列位置、撤单流或部署前历史L2的逻辑均不可编译、不可回测、不可晋级。

## 内部协同与系统可解性体检（2026-07-22）

1. 历史初筛记录只有聚合回测证据，不存在可与部署后微观遥测对齐的历史“死亡时刻”。系统禁止把AI否决发生时的当前盘口冒充策略信号环境，也不使用未来价格给未标注聚类贴结果标签。
2. 每次初筛归档同时保存受控事件与环境定义。慢微观发现服务随后在前向闭合K线上继续监测这些已死亡模式；只有真实信号时间与15分钟内的微观窗口重合，才写入 `death_micro_cooccurrence_observations`。部署前信号绝不回填。
3. 共现报告比较“某死因的同期聚类频率”和“该标的全部微观窗口背景频率”，记录命中数、分母、lift和探索性95%区间。由于不同模式与滚动窗口会重叠，它不是独立样本显著性检验，更不是因果证明。少于20个同期死因信号、聚类命中少于5个、背景少于80个，或lift下界不超过1时，一律标记样本不足。
4. 只有达到上述描述性门槛的共现单元才会交给DeepSeek复述，且只允许生成“未来影子观察警告”。它不能给聚类命名、不能成为历史DSL过滤器、不能自动生成剪枝规则。受控变异AI可以读取全部未标注聚类频率与持续性，但 `avoid_cluster_ids` 只接受已达到前向共现门槛的ID，并且仍只用于影子阶段观察。
5. 系统可解性体检是“每周 + 事件驱动”的健康心跳。确定性见证每次都免调用AI重算；三AI完整审计仅在新自然周、生效剪枝规则变化、新受控变异进入探针，或去重后独立死亡证据累积至少12条且达上次存量20%门槛时触发。无结构变化时直接读取本周最新缓存，不重复消耗API。审计记录按追加写入保存，不再用周桶覆盖；非健康态另存报警快照供Codex回溯。
6. 确定性引擎构造一个同时满足事件图谱、三AI探针、三倍成本、滚动/严重成本/容量、七日影子、Codex与人工门槛的结构性见证，以证明规则不在纯逻辑上互斥；该见证不代表市场中真实存在对应样本，也不证明现实机会不会被误杀。
7. DeepSeek、Qwen、OpenAI独立判断规则空间为 `FEASIBLE`、`EMPTY` 或 `UNCERTAIN`，并必须指出具体互斥规则。任何一家出现不可解或不确定，都会进入Codex与人工回溯并通过现有WxPusher通道预警。无论任何结论，系统都不会自动降低成本、胜率、样本、止损、容量或人工批准门槛。
8. 受控变异在候选位于同一获取价值档位时优先认知空白：没有死亡地图记录、没有达标的前向死亡-微观共现警告，且仍能用现有DSL和登记算子完整描述的方向优先获得搜索顺序。明显更高的实证获取价值仍优先，避免“为了新颖而新颖”。这只是相同门槛下的算力排序，不提高AI评分、不改变探针通过条件，不把未标注聚类冒充成可解释特征。
